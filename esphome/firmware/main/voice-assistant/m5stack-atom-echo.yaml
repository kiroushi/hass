---
packages:
  voice_assistant:
    url: github://esphome/firmware/voice-assistant/m5stack-atom-echo.yaml@main
    ref: main
    refresh: 0s
  
esphome:
  name: ${name}
  friendly_name: ${friendly_name}

api:
  encryption:
    key: ${encryption_key}

wifi:
  ssid: ${wifi_ssid}
  password: ${wifi_password}

  manual_ip:
    static_ip: ${ip_address}
    gateway: ${gateway}
    subnet: ${subnet}
